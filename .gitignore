.vercel
.env.local
.env
.env*.local
.env.vercel

# Dependencies (never commit — root node_modules alone is ~526MB)
node_modules/

# AutomateDojo is its own git repo (kennyhyder/automatedojo) nested here
automatedojo/

# AG2020 raw client data (bank statements, reports) — NEVER commit/deploy;
# Vercel serves committed static files publicly
clients/ag2020/data/

# Dunham litigation artifacts — local only, never push (>100MB PDFs)
clients/dunham/rebuttal2/
clients/dunham/*.pdf
clients/dunham/*.docx
clients/dunham/exports.zip

# Dunham litigation + client data exports — local only, never deploy
clients/dunham/rebuttal2/
clients/dunham/exports/
clients/dunham/*.pdf
clients/dunham/*.docx
clients/dunham/*.zip
clients/dunham/rebuttal-notes-*.json
clients/dunham/expert-rebuttal-report*.html
clients/dunham/rebuttal-workbook.html
