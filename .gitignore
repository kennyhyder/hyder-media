.vercel
.env.local
.env
.env*.local
.env.vercel

# Dependencies (never commit — root node_modules alone is ~526MB)
node_modules/

# AutomateDojo is its own git repo (kennyhyder/automatedojo) nested here
automatedojo/

# AG2020 raw client data (bank statements, reports) — NEVER commit/deploy;
# Vercel serves committed static files publicly
clients/ag2020/data/
