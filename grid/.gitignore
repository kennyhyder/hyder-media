# Dependencies
node_modules/

# Build temp/cache directories
out/
.next/
*.tsbuildinfo

# Environment
.env
.env.local
.env*.local

# Raw data files (too large for git) — anchored to grid root so it does NOT
# match src/data/ (the committed rollups.json the pages render from).
/data/

# Specs (documentation, not deployed)
specs/

# OS
.DS_Store

# Demo screen recordings + screenshots (large, not part of the app)
*.mov
docs/
.vercel
