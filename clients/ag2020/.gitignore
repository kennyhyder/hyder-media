# Build artifacts
node_modules/
.next/
.DS_Store

# Source files (keep only built output)
src/
scripts/
package.json
package-lock.json
next.config.js
tailwind.config.js
postcss.config.js
tsconfig.json
next-env.d.ts
public/

# data/ is NOT tracked (overridden root rule removed 2026-07-15): Vercel
# serves every committed static file publicly, and data/ holds bank
# statements + client financials. Keep local copies backed up outside git.
